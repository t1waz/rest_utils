[tox]
envlist = py37

[testenv]
deps =
    starlette
    requests
    tortoise-orm==0.16.5
    coverage
commands =
    pip install -e .
    coverage run --source serializer --source tortoise_rest_utils.view,tortoise_rest_utils.serializer -m tests.tests
    coverage report -m
